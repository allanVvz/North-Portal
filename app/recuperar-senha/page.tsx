"use client";
import {useState} from "react";import Link from "next/link";import {createClient} from "@/lib/supabase/client";
export default function RecuperarSenhaPage(){const[email,setEmail]=useState(""),[busy,setBusy]=useState(false),[sent,setSent]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);await createClient().auth.resetPasswordForEmail(email,{redirectTo:typeof window!=="undefined"?`${window.location.origin}/login`:undefined});setBusy(false);setSent(true)}return <main className="auth-screen" data-theme="dark"><span className="auth-ring" aria-hidden/><div className="auth-blob a" aria-hidden/><div className="auth-blob b" aria-hidden/><Link href="/" className="auth-back">← Voltar ao site</Link>{sent?<div className="auth-card" style={{textAlign:"center",alignItems:"center"}}><span className="auth-check" aria-hidden>✓</span><h1 className="auth-title">Link <span className="accent">enviado.</span></h1><p className="auth-lead">Se existir uma conta para <strong>{email}</strong>, você receberá as instruções de redefinição.</p><Link href="/login" className="auth-submit">Voltar ao login</Link></div>:<form className="auth-card" onSubmit={submit}><div className="auth-brand"><span className="auth-mark" aria-hidden/><strong className="wordmark">north</strong><em>Portal</em></div><h1 className="auth-title">Recuperar <span className="accent">acesso.</span></h1><p className="auth-lead">Informe seu e-mail para receber o link de redefinição.</p><label className="auth-field"><span>E-mail</span><input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="voce@empresa.com.br"/></label><button className="auth-submit" type="submit" disabled={busy}>{busy?"Enviando…":"Enviar link de acesso"}</button><Link href="/login" className="auth-help">← Voltar ao login</Link></form>}<span className="auth-pagefoot">North · estratégia &amp; operação</span></main>}
