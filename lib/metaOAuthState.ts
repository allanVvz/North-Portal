// Shared between the connect and callback routes — kept out of route.ts files
// since Next.js only allows specific named exports (GET/POST/...) there.
export const META_OAUTH_STATE_COOKIE = "meta_oauth_state";
